# frozen_string_literal: true

module PromptAtelier
  # Application version. `build` (18.8) writes it together with date and
  # commit into the VERSION file of the delivery package; this is the source.
  VERSION = '1.0.0'
end
